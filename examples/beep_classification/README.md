## 嘟声检测

```text
在通话中检测 "嘟声". 

标注, 是对每 2 秒的音频标注是否 bell. 因此属于弱标注. 
模型选用: SVM. 
方案: 
1. 音频转换频谱图. 
2. 频谱图每 n 帧截取一个向量. 向量的标签为音频的标签. (其中有些向量不是 bell, 但标签给定为 bell). 
3. 对向量做 SVM 分类. 
4. 根据 SVM 模型对各向量分类, 对向量重新打标签. 
5. 根据重新分配的标签对向量分类, 做 SVM 模型. 
6. 重复以上 4, 5 步. k 次. 


```

